/* Prints a triangle of stars n high
|    *    |
|   ***   |
|  *****  |
| ******* |
|*********|
*/

func triangle(n) {
	i => {
		if i >= n {
			return
		}
		sp = n - i - 1
		st = 2 * i + 1
		println("|" + " " * sp + "*" * st + " " * sp + "|")
		self(i + 1)
	}(0)
}

triangle(5)
